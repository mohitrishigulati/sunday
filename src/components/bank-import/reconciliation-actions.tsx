"use client";
import { useState, useTransition } from "react";
import { createBankReconciliation, setBankLineMatch } from "@/lib/actions/bank-import";
import { Button, Input, Select } from "@/components/ui/primitives";

export function BankLineMatcher({ lineId, companyId, vouchers }: { lineId: string; companyId: string; vouchers: Array<{ id: string; company_id: string; voucher_number: string; voucher_date: string }> }) { const [voucherId,setVoucherId]=useState(""); const [error,setError]=useState<string|null>(null); const [pending,startTransition]=useTransition(); const available=vouchers.filter((v)=>v.company_id===companyId); const run=(ignore:boolean)=>startTransition(async()=>{setError(null);const result=await setBankLineMatch(lineId,voucherId||undefined,ignore);if(!result.ok)setError(result.error);}); return <div className="min-w-64"><div className="flex gap-2"><select className="min-w-36 rounded border px-2 py-1" value={voucherId} onChange={(e)=>setVoucherId(e.target.value)}><option value="">Voucher</option>{available.map((v)=><option key={v.id} value={v.id}>{v.voucher_number} — {v.voucher_date}</option>)}</select><Button disabled={pending||!voucherId} onClick={()=>run(false)}>Match</Button><Button variant="ghost" disabled={pending} onClick={()=>run(true)}>Ignore</Button></div>{error?<p className="text-xs text-[var(--danger)]">{error}</p>:null}</div>; }
export function ReconciliationForm({ accounts }: { accounts: Array<{ id: string; account_name: string; account_number: string }> }) { const [error,setError]=useState<string|null>(null); const [message,setMessage]=useState<string|null>(null); const [pending,startTransition]=useTransition(); return <form className="grid gap-3 rounded-lg border border-[var(--border)] bg-[var(--surface)] p-4 md:grid-cols-4" action={(fd)=>startTransition(async()=>{setError(null);setMessage(null);const result=await createBankReconciliation({bankAccountId:String(fd.get("bankAccountId")),asOfDate:String(fd.get("asOfDate")),statementClosing:Number(fd.get("statementClosing"))});if(!result.ok){setError(result.error);return;}setMessage(`Saved. Difference: ${result.data.difference.toFixed(4)}`);})}><Select label="Bank account" name="bankAccountId" required><option value="">Select</option>{accounts.map((a)=><option key={a.id} value={a.id}>{a.account_name} — {a.account_number}</option>)}</Select><Input label="As of date" name="asOfDate" type="date" required/><Input label="Statement closing" name="statementClosing" type="number" step="0.0001" required/><div className="self-end"><Button type="submit" disabled={pending}>Reconcile</Button></div>{error?<p className="text-sm text-[var(--danger)] md:col-span-4">{error}</p>:null}{message?<p className="text-sm text-[var(--accent)] md:col-span-4">{message}</p>:null}</form>; }
