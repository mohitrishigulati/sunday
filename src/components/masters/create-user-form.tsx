"use client";import{useState,useTransition}from"react";import{createApplicationUser}from"@/lib/actions/access";import{Button,Input}from"@/components/ui/primitives";
export function CreateUserForm(){const[error,setError]=useState<string|null>(null);const[message,setMessage]=useState<string|null>(null);const[pending,startTransition]=useTransition();return <form className="grid gap-3 rounded-lg border border-[var(--border)] bg-[var(--surface)] p-4 md:grid-cols-4" action={(fd)=>startTransition(async()=>{setError(null);setMessage(null);const r=await createApplicationUser({fullName:String(fd.get("fullName")),email:String(fd.get("email")),password:String(fd.get("password"))});if(!r.ok){setError(r.error);return}setMessage(r.data.confirmationRequired?"User created; email confirmation is required before login.":"User created and ready for role/access assignment.")})}><Input label="Full name" name="fullName" required/><Input label="Email" name="email" type="email" required/><Input label="Initial password" name="password" type="password" minLength={10} required/><div className="self-end"><Button type="submit" disabled={pending}>Create user</Button></div>{error?<p className="text-sm text-[var(--danger)] md:col-span-4">{error}</p>:null}{message?<p className="text-sm text-[var(--accent)] md:col-span-4">{message}</p>:null}</form>}
